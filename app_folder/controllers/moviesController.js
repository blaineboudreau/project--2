// Dependencies
var express = require('express');
var router = express.Router();

var Movie = require('../models/movies')
var User = require('../models/users');
// Index route (GET)

// use mongoose.connection.close(); before enf of routes

// New route (GET)


// Create route (POST)


// Edit route (GET)


// Update route (PUT)


// Show route (GET)


// Delete route (DELETE)


// route movie to user show page in collection after user types in a movie and hits send button ? (PUT)



module.exports = router;
